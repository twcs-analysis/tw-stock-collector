# 資料驗證規範

本文件定義台股資料收集系統的資料品質驗證標準與驗證方法。

## 📋 目錄

- [驗證層級](#驗證層級)
- [驗證項目](#驗證項目)
- [驗證標準](#驗證標準)
- [隨機抽樣比對機制](#隨機抽樣比對機制)
- [驗證工具](#驗證工具)

---

## 🎯 驗證層級

資料驗證分為三個層級：

### Level 1: 基本結構驗證
**目的**: 確保檔案格式正確、可讀取
**執行時機**: 每次資料收集後
**驗證項目**:
- 檔案是否存在
- JSON 格式是否正確
- 檔案大小是否合理

### Level 2: 資料完整性驗證
**目的**: 確保資料內容完整、欄位齊全
**執行時機**: 每次資料收集後
**驗證項目**:
- metadata 是否完整
- 必要欄位是否存在
- 資料筆數是否合理

### Level 3: 數值合理性驗證
**目的**: 確保資料數值符合商業邏輯
**執行時機**: 每日定期驗證 + 回補後驗證
**驗證項目**:
- 數值範圍是否合理
- 數值邏輯是否正確
- 與歷史資料是否一致

---

## ✅ 驗證項目

### 1. 基本結構驗證

#### 1.1 檔案存在性檢查

```python
# 檢查項目
- 檔案路徑: data/raw/{type}/{YYYY}/{MM}/{YYYY-MM-DD}.json
- 檔案大小: 最小 10KB, 最大 10MB

# 驗證規則
PASS: 檔案存在且大小在合理範圍內
FAIL: 檔案不存在或大小異常
WARN: 檔案大小偏離平均值 ±50%
```

#### 1.2 JSON 格式驗證

```python
# 檢查項目
- 是否為有效 JSON
- 是否符合預期結構 (metadata + data)

# 驗證規則
PASS: JSON 格式正確且包含 metadata 和 data
FAIL: JSON 格式錯誤或缺少必要欄位
```

### 2. Metadata 驗證

#### 2.1 必要欄位檢查

```json
{
  "metadata": {
    "date": "YYYY-MM-DD",           // 必要，格式正確
    "collected_at": "YYYY-MM-DD HH:MM:SS", // 必要，格式正確
    "total_count": 1000,             // 必要，正整數
    "source": "TWSE + TPEx Official API"    // 必要，字串
  }
}
```

**驗證規則**:
- `date`: 必須符合 `YYYY-MM-DD` 格式
- `collected_at`: 必須符合 `YYYY-MM-DD HH:MM:SS` 格式
- `total_count`: 必須為正整數，範圍 500~3000
- `source`: 必須包含 "TWSE" 或 "TPEx"

#### 2.2 日期一致性檢查

```python
# 檢查項目
- 檔案名稱中的日期 = metadata.date
- metadata.collected_at >= metadata.date

# 驗證規則
PASS: 日期一致
FAIL: 日期不一致
```

### 3. 資料完整性驗證

#### 3.1 資料筆數檢查

| 資料類型 | 中文名稱 | 最小筆數 | 預期筆數 | 最大筆數 |
|---------|---------|---------|---------|---------|
| price | 價格資料 | 1,500 | 1,946 | 2,500 |
| institutional | 三大法人 | 800 | 1,027 | 1,500 |
| margin | 融資融券 | 1,500 | 1,815 | 2,500 |
| lending | 借券賣出 | 800 | 1,046 | 1,500 |

**驗證規則**:
- `PASS`: 筆數在最小~最大範圍內
- `WARN`: 筆數在範圍內但偏離預期值 ±20%
- `FAIL`: 筆數小於最小值或大於最大值

#### 3.2 必要欄位檢查

##### Price (價格資料)
```python
required_fields = [
    'date',          # 日期
    'stock_id',      # 股票代碼
    'stock_name',    # 股票名稱
    'open',          # 開盤價
    'high',          # 最高價
    'low',           # 最低價
    'close',         # 收盤價
    'volume',        # 成交量
    'type'           # 市場類型 (twse/tpex)
]

# 驗證規則
- 所有欄位必須存在
- 允許空值的欄位: 無（所有欄位都必須有值）
- 資料型態必須正確
```

##### Institutional (三大法人)
```python
required_fields = [
    'date',                    # 日期
    'stock_id',                # 股票代碼
    'stock_name',              # 股票名稱
    'foreign_buy',             # 外資買進
    'foreign_sell',            # 外資賣出
    'foreign_net',             # 外資買賣超
    'trust_buy',               # 投信買進
    'trust_sell',              # 投信賣出
    'trust_net',               # 投信買賣超
    'dealer_buy',              # 自營商買進
    'dealer_sell',             # 自營商賣出
    'dealer_net',              # 自營商買賣超
    'total_net',               # 三大法人買賣超合計
    'type'                     # 市場類型
]
```

##### Margin (融資融券)
```python
required_fields = [
    'date',                    # 日期
    'stock_id',                # 股票代碼
    'stock_name',              # 股票名稱
    'margin_balance',          # 融資餘額
    'margin_change',           # 融資增減
    'short_balance',           # 融券餘額
    'short_change',            # 融券增減
    'type'                     # 市場類型
]
```

##### Lending (借券賣出)
```python
required_fields = [
    'date',                    # 日期
    'stock_id',                # 股票代碼
    'stock_name',              # 股票名稱
    'lending_balance',         # 借券餘額
    'lending_change',          # 借券增減
    'type'                     # 市場類型
]
```

#### 3.3 空值檢查

```python
# 檢查項目
- 必要欄位不得為 null、空字串、NaN

# 驗證規則
PASS: 所有必要欄位都有值
WARN: 非必要欄位有空值（記錄但不影響）
FAIL: 必要欄位有空值
```

### 4. 數值合理性驗證

#### 4.1 價格資料驗證

```python
# 價格範圍
price_ranges = {
    'open':   (0.01, 10000),    # 開盤價
    'high':   (0.01, 10000),    # 最高價
    'low':    (0.01, 10000),    # 最低價
    'close':  (0.01, 10000),    # 收盤價
    'volume': (0, 1000000000)   # 成交量
}

# 價格邏輯
validation_rules = {
    'high_low': 'high >= low',           # 最高價 >= 最低價
    'high_open': 'high >= open',         # 最高價 >= 開盤價
    'high_close': 'high >= close',       # 最高價 >= 收盤價
    'low_open': 'low <= open',           # 最低價 <= 開盤價
    'low_close': 'low <= close',         # 最低價 <= 收盤價
    'volume_positive': 'volume >= 0'     # 成交量 >= 0
}

# 異常值檢測
- 漲跌幅超過 ±20% 需標記
- 成交量為 0 需標記（可能停牌）
```

#### 4.2 法人資料驗證

```python
# 數值範圍
institutional_ranges = {
    'buy':  (-1e12, 1e12),     # 買進金額
    'sell': (-1e12, 1e12),     # 賣出金額
    'net':  (-1e12, 1e12)      # 買賣超
}

# 邏輯驗證
validation_rules = {
    'net_calculation': 'net = buy - sell',  # 買賣超 = 買進 - 賣出
    'total_sum': 'total_net = foreign_net + trust_net + dealer_net'
}

# 異常值檢測
- 單日買賣超超過 10 億需標記
```

#### 4.3 融資融券驗證

```python
# 數值範圍
margin_ranges = {
    'balance': (0, 1e12),      # 餘額必須 >= 0
    'change':  (-1e12, 1e12)   # 增減可正可負
}

# 異常值檢測
- 單日增減超過 ±50% 需標記
- 餘額突然歸零需標記
```

### 5. 跨日資料一致性驗證

#### 5.1 餘額連續性檢查

```python
# 檢查邏輯（以融資為例）
today_balance = today.margin_balance
yesterday_balance = yesterday.margin_balance
today_change = today.margin_change

# 驗證規則
expected_balance = yesterday_balance + today_change
tolerance = 1000  # 允許誤差

if abs(today_balance - expected_balance) > tolerance:
    status = WARN  # 可能有除權息或其他因素
```

#### 5.2 歷史資料比對

```python
# 檢查項目
- 同一股票在不同日期的資料是否存在異常跳動
- 價格突然暴漲暴跌（±50%）
- 成交量突然暴增（超過平均值 10 倍）

# 驗證規則
PASS: 波動在合理範圍內
WARN: 波動較大但可能正常（除權息、重大事件）
FAIL: 波動超出合理範圍，需人工確認
```

---

## 🎲 隨機抽樣比對機制

### 設計原則

1. **分層抽樣**: 依市場類型（上市/上櫃）和產業分類進行分層
2. **隨機性**: 使用偽隨機數生成器確保可重現性
3. **覆蓋率**: 確保各類型股票都被抽樣到
4. **比對頻率**: 每日自動比對 + 人工觸發比對

### 抽樣策略

#### 策略 1: 固定數量抽樣

```python
# 每日抽樣配額
daily_sample_size = {
    'price': 20,          # 抽樣 20 檔股票
    'institutional': 15,  # 抽樣 15 檔股票
    'margin': 15,         # 抽樣 15 檔股票
    'lending': 10         # 抽樣 10 檔股票
}

# 抽樣分布
sample_distribution = {
    'twse': 0.6,  # 60% 上市股票
    'tpex': 0.4   # 40% 上櫃股票
}

# 抽樣權重（優先抽樣重要股票）
sample_weights = {
    'large_cap': 0.5,    # 50% 大型股（市值前 100）
    'mid_cap': 0.3,      # 30% 中型股
    'small_cap': 0.2     # 20% 小型股
}
```

#### 策略 2: 分層隨機抽樣

```python
# 第一層: 市場類型
markets = ['twse', 'tpex']

# 第二層: 產業分類
industries = [
    '半導體業', '電腦及週邊設備業', '光電業',
    '通信網路業', '金融保險業', '航運業'
]

# 抽樣方法
def stratified_sampling(data, n_samples=20):
    """
    分層抽樣

    1. 依市場類型分組
    2. 依產業分類分組
    3. 每個群組抽樣 ceiling(n_samples / n_groups)
    4. 確保至少每個群組有 1 個樣本
    """
    samples = []

    # 市場分層
    for market in markets:
        market_data = data[data['type'] == market]

        # 產業分層
        for industry in industries:
            industry_data = market_data[
                market_data['industry'] == industry
            ]

            if len(industry_data) > 0:
                # 隨機抽 1~2 檔
                n = min(2, len(industry_data))
                sample = industry_data.sample(n=n, random_state=seed)
                samples.append(sample)

    return pd.concat(samples).head(n_samples)
```

#### 策略 3: 指標股票必抽

```python
# 必抽股票清單（指標股）
must_check_stocks = [
    '2330',  # 台積電
    '2317',  # 鴻海
    '2454',  # 聯發科
    '2412',  # 中華電
    '2882',  # 國泰金
    '2891',  # 中信金
    '2303',  # 聯電
    '2308',  # 台達電
    '1301',  # 台塑
    '1303'   # 南亞
]

# 抽樣邏輯
def smart_sampling(data, n_samples=20):
    """
    智能抽樣

    1. 必抽股票: 10 檔指標股（固定）
    2. 隨機抽樣: 10 檔一般股票（隨機）
    """
    # 必抽樣本
    must_samples = data[data['stock_id'].isin(must_check_stocks)]

    # 隨機樣本
    remaining_data = data[~data['stock_id'].isin(must_check_stocks)]
    random_samples = remaining_data.sample(
        n=n_samples - len(must_samples),
        random_state=seed
    )

    return pd.concat([must_samples, random_samples])
```

### 比對方法

#### 方法 1: 直接比對官網資料

```python
# 比對流程
1. 從抽樣清單中選取股票
2. 呼叫 TWSE/TPEx API 獲取官方資料
3. 比對收集的資料與官方資料
4. 計算差異並評分

# 比對項目
- 價格資料: open, high, low, close, volume
- 法人資料: foreign_net, trust_net, dealer_net
- 融資融券: margin_balance, short_balance
- 借券賣出: lending_balance

# 容差設定
tolerance = {
    'price': 0.01,      # 價格允許 0.01 元誤差
    'volume': 1000,     # 成交量允許 1000 股誤差
    'amount': 10000     # 金額允許 1 萬元誤差
}
```

#### 方法 2: 交叉驗證

```python
# 驗證邏輯
1. 從兩個不同來源收集同一天的資料
2. 比對兩份資料是否一致
3. 如有差異，以官網為準

# 資料來源
source_a = "TWSE/TPEx Official API"
source_b = "FinMind API"  # 輔助驗證

# 一致性評分
consistency_score = (matched_count / total_count) * 100
```

#### 方法 3: 邏輯驗證

```python
# 驗證項目
1. 當日成交量加總 = metadata.total_volume（如有提供）
2. 三大法人買賣超合計 = foreign + trust + dealer
3. 餘額 = 前日餘額 + 當日增減

# 驗證結果
PASS: 所有邏輯驗證通過
WARN: 部分驗證未通過但可接受
FAIL: 關鍵驗證未通過
```

### 比對報告

```python
# 報告格式
validation_report = {
    'date': '2025-12-28',
    'data_type': 'price',
    'sample_size': 20,
    'validation_results': {
        'total_samples': 20,
        'passed': 18,
        'warned': 2,
        'failed': 0,
        'accuracy': 90.0  # (passed / total) * 100
    },
    'failed_stocks': [],
    'warned_stocks': [
        {
            'stock_id': '2330',
            'field': 'volume',
            'our_value': 45000000,
            'official_value': 45123000,
            'diff': 123000,
            'diff_pct': 0.27
        }
    ]
}
```

---

## 🔧 驗證工具

### 命令行工具

```bash
# 驗證單一檔案
python scripts/validate_data.py --file data/raw/price/2025/12/2025-12-28.json

# 驗證指定日期
python scripts/validate_data.py --date 2025-12-28 --type price

# 驗證日期範圍
python scripts/validate_data.py --start 2025-12-01 --end 2025-12-31

# 隨機抽樣比對
python scripts/validate_data.py --date 2025-12-28 --sample 20 --compare-official

# 生成驗證報告
python scripts/validate_data.py --date 2025-12-28 --report validation_report.html
```

### GitHub Actions 自動驗證

```yaml
# .github/workflows/data-validation.yml
name: Data Validation

on:
  schedule:
    - cron: '0 19 * * 1-5'  # 每交易日 19:00 執行
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate daily data
        run: |
          python scripts/validate_data.py \
            --date $(date +%Y-%m-%d) \
            --sample 20 \
            --report
```

---

## 📊 品質指標

### KPI 定義

| 指標 | 目標值 | 警戒值 | 說明 |
|------|--------|--------|------|
| **資料完整率** | ≥ 99% | < 95% | 必要欄位的完整程度 |
| **格式正確率** | 100% | < 100% | JSON 格式與結構正確性 |
| **數值準確率** | ≥ 99.5% | < 98% | 與官方資料比對的準確度 |
| **邏輯一致率** | ≥ 99% | < 95% | 資料邏輯驗證通過率 |
| **每日驗證覆蓋率** | ≥ 1% | < 0.5% | 每日抽樣驗證的股票比例 |

### 品質評級

```python
# 綜合評分計算
quality_score = (
    completeness * 0.3 +    # 完整率 30%
    format_correct * 0.2 +   # 格式正確率 20%
    accuracy * 0.3 +         # 準確率 30%
    consistency * 0.2        # 一致率 20%
)

# 評級標準
A+ : quality_score >= 99.0  # 優秀
A  : quality_score >= 95.0  # 良好
B  : quality_score >= 90.0  # 合格
C  : quality_score >= 80.0  # 需改進
D  : quality_score < 80.0   # 不合格
```

---

## 🚨 異常處理

### 異常分類

#### 輕微異常 (WARN)
- 資料筆數偏離預期 ±10%
- 個別股票價格波動較大（±10%~20%）
- 成交量為 0（可能停牌）

**處理方式**: 記錄日誌，不影響後續流程

#### 中度異常 (ERROR)
- 資料筆數偏離預期 ±20%
- 必要欄位有空值（< 5% 資料）
- 價格邏輯錯誤（如 high < low）

**處理方式**: 發送警告通知，標記問題資料

#### 嚴重異常 (CRITICAL)
- 檔案不存在或無法讀取
- JSON 格式錯誤
- 資料筆數 < 最小值
- 必要欄位大量空值（> 5%）

**處理方式**: 發送緊急通知，停止後續流程，需人工介入

### 修復機制

```python
# 自動修復（僅限輕微異常）
auto_fix_rules = {
    'trim_whitespace': True,     # 移除前後空白
    'fill_zero_volume': True,    # 成交量空值填 0
    'infer_missing_type': True   # 從股票代碼推斷市場類型
}

# 人工修復（中度以上異常）
manual_fix_required = {
    'price_logic_error': True,   # 價格邏輯錯誤
    'missing_required_field': True,  # 缺少必要欄位
    'file_corruption': True      # 檔案損壞
}
```

---

## 📝 驗證紀錄

### 日誌格式

```json
{
  "timestamp": "2025-12-28T19:00:00",
  "validation_type": "daily",
  "date": "2025-12-28",
  "data_types": ["price", "institutional", "margin", "lending"],
  "summary": {
    "total_files": 4,
    "passed": 4,
    "warned": 0,
    "failed": 0
  },
  "details": [
    {
      "data_type": "price",
      "file_path": "data/raw/price/2025/12/2025-12-28.json",
      "validations": {
        "structure": "PASS",
        "metadata": "PASS",
        "completeness": "PASS",
        "values": "PASS",
        "sampling": {
          "sample_size": 20,
          "matched": 20,
          "accuracy": 100.0
        }
      }
    }
  ]
}
```

### 報告儲存

```bash
# 驗證報告目錄結構
logs/validation/
├── daily/
│   ├── 2025-12-28_validation.json
│   └── 2025-12-28_validation.html
├── weekly/
│   └── 2025-W52_summary.json
└── monthly/
    └── 2025-12_summary.json
```

---

## 🔄 持續改進

### 定期檢視

- **每日**: 查看驗證報告，處理異常
- **每週**: 統計驗證指標，分析趨勢
- **每月**: 評估驗證規則，調整標準
- **每季**: 檢討驗證機制，優化流程

### 版本控制

```markdown
## 版本歷史

### v1.0.0 (2025-12-28)
- 初版發布
- 定義基本驗證規則
- 實作隨機抽樣機制

### v1.1.0 (計畫中)
- 增加交叉驗證
- 優化抽樣策略
- 加強異常檢測
```

---

**最後更新**: 2025-12-28
**維護者**: tw-stock-collector 專案團隊
